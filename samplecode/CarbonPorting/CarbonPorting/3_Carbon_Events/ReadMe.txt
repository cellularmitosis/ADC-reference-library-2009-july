ExamplePrefs 3.0Step 3 in your Carbonizing project doesn't change anything in the Classic version of the program.  It is left in the project just to see how much code is reusable from a WaitNextEvent() program to a RunApplicationEventLoop() program.