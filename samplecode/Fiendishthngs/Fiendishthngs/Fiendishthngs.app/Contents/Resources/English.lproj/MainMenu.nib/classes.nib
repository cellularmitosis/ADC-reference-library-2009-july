{
    IBClasses = (
        {
            ACTIONS = {openComponent = id; saveReport = id; }; 
            CLASS = ComponentsController; 
            LANGUAGE = ObjC; 
            OUTLETS = {arrayController = id; compCount = id; tableView = id; textView = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}