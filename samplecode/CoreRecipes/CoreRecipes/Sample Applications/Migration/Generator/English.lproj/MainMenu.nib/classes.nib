{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {createStoreAction = id; showStoreContents = id; }; 
            CLASS = GeneratorAppDelegate; 
            LANGUAGE = ObjC; 
            OUTLETS = {storeContentTextView = NSTextView; storePathTextField = NSTextField; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}