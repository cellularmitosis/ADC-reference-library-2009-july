{
    IBClasses = (
        {
            ACTIONS = {start = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                "_itemMatrix" = NSMatrix; 
                "_numberOfItems" = NSTextField; 
                "_startButton" = NSButton; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}