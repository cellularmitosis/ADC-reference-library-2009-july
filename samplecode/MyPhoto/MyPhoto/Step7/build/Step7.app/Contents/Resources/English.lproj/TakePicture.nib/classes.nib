{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {closeWindow = id; takePicture = id; }; 
            CLASS = ICTakePictureHandler; 
            LANGUAGE = ObjC; 
            OUTLETS = {mImageView = NSImageView; mWindow = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}